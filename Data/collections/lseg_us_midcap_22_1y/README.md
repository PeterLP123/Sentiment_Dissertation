# LSEG US Mid-Cap 22 — One-Year Reuters Headlines

This directory is the local output root for the deadline-bounded exploratory
mid-cap strategy extension. The tracked collection definition is
[`configs/lseg_us_midcap_22_1y.toml`](../../../configs/lseg_us_midcap_22_1y.toml)
and the complete command sequence is in the
[`mid-cap deadline runbook`](../../../docs/midcap_deadline_runbook.md).

The generated `raw/` and `derived/` trees contain licensed LSEG headlines and
local model outputs. They are ignored by Git and must not be redistributed.
